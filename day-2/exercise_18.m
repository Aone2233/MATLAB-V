g = 9.81; % 重力加速度，单位m/s^2
v_0 = 340; % 声速，单位m/s

% r = (0.85 * v_0)^2 ./ g; % 计算r的值
% disp(r); % 显示r的值

% r = (1.5 * v_0)^2 ./ 2 * g; % 计算r的值
% disp(r); % 显示r的值

% V = 0.5 * v_0:0.1:1.5 * v_0; % 速度范围
% r = V.^2 / (2 * g); % 计算r的值
% plot(V, r); % 绘制r-V曲线

% 可以采用hold on和hold off来实现两个图像在一张图上展示

a = 2 * g:0.1:6 * g; % 加速度范围
V_1 = 0.85 * v_0; % 定义新的速度V_1
r_1 = V_1.^2 ./ a; % 计算r_1的值
plot(a, r_1); % 绘制r_1-a曲线
title('a和r的关系');
xlabel('a(m/s^2)');
ylabel('r(m)');
legend('r_1-a曲线');
grid on; % 显示网格